export const createAnnotation = (annotation, success, error) => {
  // TODO add the new annotation to the database
};

export const fetchAnnotations = (success, error) => {
  // TODO fetch the annotations from the database
};
